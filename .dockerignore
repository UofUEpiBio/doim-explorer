# Google WIF credentials are generated transiently by the deployment action. They are
# not needed by the Docker build and must never enter an image layer.
gha-creds-*.json

.git/
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
